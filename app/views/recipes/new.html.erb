<div class="w-full flex-col justify-around">

  <%= form_for :recipe, url: "/recipes", class: "w-full flex flex-col justify-around px-5"  do |r| %>
    <div class="flex flex-col w-full m-2">
      <%= r.label "recipe name", class: "w-full text-white text-lg" %>
      <%= r.text_field :name, class: "bg-white p-2 rounded-xl"%>
    </div>

    <div class="flex flex-col w-full m-2">
      <%= r.label "description", class: "w-full text-white text-lg" %>
      <%= r.text_area :description, class: "bg-white p-2 rounded-xl h-40"%>
    </div>

    <div class="flex flex-col w-full m-2">
      <%= r.label "sources", class: "w-full text-white text-lg" %>
      <%= r.text_field :sources, class: "bg-white p-2 rounded-xl"%>
    </div>

    <%= r.submit "Add Ingredients", class: "bg-blue-500 p-2 rounded-lg w-40 mx-2 " %>

  <% end %>

</div>
